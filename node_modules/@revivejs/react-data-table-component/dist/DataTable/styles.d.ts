import { TableStyles, TableTheme, Theme, Themes } from './types';
export declare const defaultStyles: (theme: Theme) => TableTheme;
export declare const createStyles: (customStyles?: TableStyles, themeName?: string, inherit?: Themes) => TableTheme;
